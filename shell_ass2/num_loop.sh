#!/bin/bash
#
for num in {1..20}; do
  echo "$num"
    if (( num % 2 == 0 )); then
           echo "I'm big!"
          fi
          done
	  #execution procedure
	  #chmod u+x num_loop.sh
	  #./num_loop.sh
         
